# Session Summary: 2026-05-03

## What We Did

### TLS + Domain (Step 4 of deploy-k3s.md)

Steps 1–3 (k3s, ArgoCD, Envoy Gateway) were already complete at the start of this session.

**Networking approach:** No static IP available. Used Tailscale instead — the machine's stable Tailscale IP (`100.92.225.18`) is the target for all external DNS.

**DNS:** Added an A record via the Porkbun API:
- `longwood.daphnai.com` → `100.92.225.18` (Tailscale IP), TTL 600s

**TLS stack deployed via ArgoCD:**
- cert-manager v1.20.2 (`charts.jetstack.io`)
- cert-manager-webhook-porkbun v1.0.0 by Talinx (`talinx.github.io/cert-manager-webhook-porkbun`)
  - groupName: `porkbun.talinx.dev`

**Kubernetes resources created:**
- `Secret/porkbun-secret` (cert-manager ns) — Porkbun API credentials (gitignored)
- `ClusterIssuer/letsencrypt-prod` — Let's Encrypt ACME with DNS-01 via Porkbun
- `Certificate/longwood-daphnai-com` (default ns) — valid until 2026-08-01
- `Gateway/longwood` (default ns) — Envoy Gateway, port 443, TLS terminate, hostname `longwood.daphnai.com`

### ArgoCD HTTPRoute

ArgoCD UI routed to `https://longwood.daphnai.com/argocd`.

**Changes required to make subpath routing work:**
- Patched `argocd-cmd-params-cm` with `server.rootpath: /argocd` and `server.insecure: "true"` (insecure needed because TLS is terminated at the gateway — ArgoCD receives plain HTTP)
- Restarted `argocd-server` deployment

**New manifests:**
- `manifests/argocd-referencegrant.yaml` — allows `default` ns HTTPRoute to reference `argocd-server` in `argocd` ns
- `manifests/argocd-httproute.yaml` — routes `/argocd` prefix to `argocd-server:80`

## Current State

| Resource | Status |
|----------|--------|
| k3s single-node cluster | Running |
| ArgoCD | https://longwood.daphnai.com/argocd |
| Envoy Gateway | Programmed, 192.168.1.184 |
| TLS cert | Valid, auto-renews via cert-manager |
| DNS | longwood.daphnai.com → 100.92.225.18 |

## Pending / Next Steps

- [ ] Rotate Porkbun API keys (were shared in plaintext during session)
- [ ] Change ArgoCD admin password and delete `argocd-initial-admin-secret`
- [ ] Deploy first workload app and add an HTTPRoute for it
